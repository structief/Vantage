{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "github-contents",
  "title": "GitHub Contents API — consumed fields",
  "description": "Shape of each item in GET /repos/{owner}/{repo}/contents/{path} response array. Only the fields actually consumed by Vantage's projects endpoint are documented here.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["name", "type"],
    "properties": {
      "name": {
        "type": "string",
        "description": "File or directory name, e.g. 'repo-sidebar-navigation'"
      },
      "type": {
        "type": "string",
        "enum": ["file", "dir", "symlink", "submodule"],
        "description": "Entry type. Vantage filters to 'dir' for change directories and 'file' for spec files."
      }
    }
  }
}
